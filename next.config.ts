import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Remote fashion imagery is served via plain <img> tags (design preserved as-is),
  // so the next/image remote allow-list is not required here.
};

export default nextConfig;
